/*
import { api } from "../service/apiHelper";

 export const deleteComplexApi = async (complexOfBuilding) => {
  const response = await api.deletePrivate('/complex-of-buildings/delete-complex-of-buildings', complexOfBuilding);
  return response;
};
 */